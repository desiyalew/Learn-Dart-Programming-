void main() {
  print("this is your first dart programming task");

  //dart variables declaration and intialization
// 1. using var
  var name;
  assert(name == 'null'); //her is the default value assignment
  //2. using data type
  int age;

  //3. using dinamicly
  dynamic variable;

  // how we inizalize variables
  name = "desiyalew";
  age = 28;
  variable = "this is dynamic var";
  //or we can also intialize when we are declar variables

// now we can access variable
//
  print(name);
  print(age);
  print(variable);
}
